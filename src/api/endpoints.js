import { API_HOST } from '../global';

export const API_ROOT = `${API_HOST}`;

export const LOGIN_ENDPOINT = `${API_ROOT}/login/`;

export const REGISTRATION_ENDPOINT = `${API_ROOT}/registration/`;


console.log(REGISTRATION_ENDPOINT);